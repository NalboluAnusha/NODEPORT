Create a Pod and Service
  apiVersion: v1
kind: Pod
metadata:
 namespace: prod
 name: node1
 labels:
  app: myapp
spec:
 containers:
 - name: c12
   image: tomcat:9.0
   ports:
   - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: node1
  namespace: prod
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
    - port: 80
 targetPort: 8080
      nodePort: 30007
🔹 Step 2: Apply YAML

kubectl apply -f nodeport.yaml
      targetPort: 8080

